# 10.1 Classified Findings — Deduplicated & Prioritized

**Task:** Consolidate all findings from automated, structural, and manual passes; deduplicate overlapping items; assign priority tiers.  
**Requirements traced:** 8.1, 8.2, 8.5

---

## Priority Classification Rules

- **HIGH:** Blocks future features, causes bugs, correctness risks — test gaps in critical paths, `expect()`/`unwrap()` in audio thread, boundary violations with production coupling, OCP violations that block feature development
- **MEDIUM:** Increases maintenance cost — DRY violations with 3+ occurrences, SRP violations, coupling hotspots, encapsulation risks, KISS violations (long functions >100 lines)
- **LOW:** Cosmetic or minor improvements — dead code, cosmetic KISS items, single-use wrappers, minor DRY (2 occurrences)

---

## Master Findings Table

| ID | Title | Priority | Category | Originating Req | File(s) | Brief Description |
|----|-------|----------|----------|-----------------|---------|-------------------|
| F-01 | `expect()`/`unwrap()` in audio output thread | **HIGH** | ErrorHandlingRisk | 7.5 | `src/audio/output_manager.rs:61,115` | Two `expect()` calls in audio output thread; panic crashes audio pipeline during device hotplug scenarios |
| F-02 | `expect()` in audio decode thread | **HIGH** | ErrorHandlingRisk | 7.5 | `src/audio/stream_source.rs:158,166` | Two `expect()` calls in stream source ICY parsing; invariant-guarded but panic terminates decode thread |
| F-03 | Test gap: `update.rs` central action dispatcher | **HIGH** | TestGap | 7.3 | `src/app/update.rs` | Main dispatcher routing all user actions has zero `#[test]` blocks; mode-gating logic (settings swallow, sleep timer isolation) is untested directly |
| F-04 | OCP violation: Visualizer mode numeric dispatch | **HIGH** | OcpViolation | 5.3, 5.7 | `src/app/overlays.rs:111`, `src/app/ui_state.rs:6`, `src/ui/deck/visualizer/mod.rs:14-52`, `src/ui/controls.rs:148-153`, `src/app/visualizer/mod.rs:12` | Raw `usize` mode with `% 3` arithmetic and wildcard `_` arms; adding a new visualizer requires modifying 4+ files; no compiler enforcement |
| F-05 | OCP violation: Overlay extension point | **HIGH** | OcpViolation | 5.1 | `src/app/overlays.rs`, `src/ui/mod.rs:111`, `src/action.rs`, `src/app/update.rs`, `src/event.rs` | Adding a new overlay requires modifying 4 existing files beyond enum + renderer; centralized match dispatch prevents extension without modification |
| F-06 | Boundary violation: `app-state` → `ui-rendering` | **HIGH** | BoundaryViolation | 6.5, 5.5 | `src/app/settings.rs:3,81-84` | `App::apply_directional_setting()` imports `ThemeName` from `crate::ui::theme` and calls `set_active()`; production coupling from app-state into ui-rendering layer |
| F-07 | Interior-mutable handle in UiModel (`sample_buffer`) | **HIGH** | BoundaryViolation | 6.3 | `src/ui/model.rs:30` | `UiModel` exposes `&Arc<Mutex<VecDeque<f32>>>` allowing renderers write access to shared audio buffer; violates read-only snapshot principle |
| F-08 | `App` module coupling hotspot (Ca=4, Ce=6) | **MEDIUM** | CouplingHotspot | 7.1 | `src/app.rs`, `src/app/*` | Only module exceeding both Ca>3 and Ce>3; changes cascade to 4 dependent modules; high instability (0.60) |
| F-09 | SRP: `UiRuntimeState` mixes audio + app-state in UI struct | **MEDIUM** | SrpViolation | 4.1, 4.4 | `src/app/ui_runtime.rs` | `visualizer_peaks` (audio-engine data), `nav` and `should_quit` (app-state) stored in a UI rendering struct |
| F-10 | SRP: `PlaybackRuntime` mixes audio-engine + infrastructure + app-state | **MEDIUM** | SrpViolation | 4.1, 4.4 | `src/app/playback_runtime.rs` | `sleep_timer` (app-state), `reconnect`/`diagnostics` (infrastructure) alongside audio engine handles |
| F-11 | SRP: Functions in `src/app/` mix state transitions with notice formatting | **MEDIUM** | SrpViolation | 4.2, 4.3 | `src/app/playback.rs`, `src/app/reconnect.rs`, `src/app/library.rs`, `src/app/settings.rs`, `src/app/sleep_timer.rs` | Every state transition function also constructs user-facing notice messages inline; prevents independent testing and future i18n |
| F-12 | DRY: Genre filtering logic duplicated 3× | **MEDIUM** | DryViolation | 2.1, 2.3 | `src/app/selectors.rs:10-27,62-79`, `src/ui/model.rs:101-125` | Identical genre-based station filtering implemented in `App::visible_stations()`, `App::visible_count()`, and `visible_stations_for()` |
| F-13 | DRY: Overlay render boilerplate repeated in 7 overlays | **MEDIUM** | DryViolation | 2.1 | `src/ui/help.rs`, `src/ui/station_details.rs`, `src/ui/recent_tracks.rs`, `src/ui/sleep_timer.rs`, `src/ui/settings.rs`, `src/ui/playback_doctor.rs`, `src/ui/command_palette.rs` | Same 7-step initialization (centered_rect → compact check → clear → block → inner_area → alert) with only title/dimensions varying |
| F-14 | DRY: Buffer/track reset triple (5 occurrences) | **MEDIUM** | DryViolation | 2.1, 2.2 | `src/app/playback.rs:31-33,82-84,121-123`, `src/app/lifecycle.rs:285-287,312-314` | `current_track=None; buffer_percent=0; buffer_seconds=0` repeated identically 5 times |
| F-15 | DRY: Play-and-sync-volume sequence (4 occurrences) | **MEDIUM** | DryViolation | 2.1, 2.2 | `src/app/playback.rs:63-65,125-127`, `src/app/lifecycle.rs:161-163`, `src/app/reconnect.rs:65-67` | `if send_audio_command(Play(url)) { sync_volume() }` repeated 4 times with same `Connecting` state transition |
| F-16 | DRY: Persistence load/save pattern (cfg-doubled) | **MEDIUM** | DryViolation | 2.1, 2.2 | `src/history.rs:44-73`, `src/app/ui_state.rs:38-96` | Identical `load_with_warning()` + `save()` + `load()` pattern × 2 types, each doubled with `cfg(test)` no-op variants (12 methods total) |
| F-17 | DRY: Find-station-by-URL pattern (13 call sites) | **MEDIUM** | DryViolation | 2.1 | `src/app/selectors.rs`, `src/app/library.rs`, `src/app/lifecycle.rs`, `src/favorites.rs` | `collection.iter().find(\|s\| station_url_matches(&s.url, target))` repeated across 4 files |
| F-18 | KISS: `run_worker` (159 logic lines) | **MEDIUM** | KissViolation | 3.1 | `src/audio/decode.rs:164-355` | Sequential 10-step streaming pipeline in single function; exceeds threshold by 3×; decomposable into 5 sub-functions |
| F-19 | KISS: `stations::render` (116 logic lines) | **MEDIUM** | KissViolation | 3.1 | `src/ui/stations.rs:10-142` | Station list renderer mixing layout computation, genre tabs, item construction, and empty state |
| F-20 | KISS: `render_visualizer_signal` (92 lines, depth 5) | **MEDIUM** | KissViolation | 3.1, 3.2 | `src/ui/deck/visualizer/mod.rs:36-140` | Four visualization modes inlined in single match with nesting depth 5 |
| F-21 | KISS: `handle_event` (88 lines, depth 5) | **MEDIUM** | KissViolation | 3.1, 3.2 | `src/audio/engine_loop_v2.rs:195-288` | Large match over `EngineEvent` variants with nested state matches reaching depth 5 |
| F-22 | Encapsulation: 8 structs with pub fields, no enforcing constructor | **MEDIUM** | EncapsulationRisk | 7.4 | `src/app/playback.rs:4`, `src/app/types.rs:55`, `src/app/nav.rs:4`, `src/app/overlays.rs:17`, `src/app/ui_runtime.rs:4`, `src/app/command_palette.rs:6`, `src/app/search.rs:5`, `src/app/playback_runtime.rs:5` | Implicit constraints (volume 0-100, buffer_percent 0-100, reconnect ≤ limit) not enforced by type or constructor |
| F-23 | Domain filtering logic in UiModel creation | **MEDIUM** | BoundaryViolation | 6.1 | `src/ui/model.rs:104-127` | `visible_stations_for()` re-implements genre filtering that belongs in app layer; DRY + separation crossover (merged with F-12) |
| F-24 | `pub(crate)` audio modules permit boundary bypass | **MEDIUM** | EncapsulationRisk | 5.6 | `src/audio.rs` (`pub(crate) mod types`, `pub(crate) mod volume`) | Visibility allows future code to bypass re-export boundary; should be `pub(super)` for compiler enforcement |
| F-25 | DRY: Error notice format pattern (6+ call sites) | **MEDIUM** | DryViolation | 2.1, 2.2 | `src/app/library.rs`, `src/app/playback.rs`, `src/app/search.rs`, `src/app/persist.rs` | `set_error_notice(format!("{context}: {err}"))` repeated with same template across 6+ sites |
| F-26 | DRY: Reconnect disarm + reset sequence (5 occurrences) | **MEDIUM** | DryViolation | 2.1 | `src/app/playback.rs:54,90`, `src/app/lifecycle.rs:192,293`, `src/app/reconnect.rs:72` | `reconnect.disarm()` always appears alongside buffer/state reset in stereotyped ceremony |
| F-27 | `unwrap()` on theme colors in UI rendering | **LOW** | ErrorHandlingRisk | 7.5 | `src/ui/settings.rs:78,203`, `src/ui/help.rs:135` | `theme::text().fg.unwrap()` / `theme::dim().fg.unwrap()` — panics if theme returns None foreground |
| F-28 | Dead code: 10 functions suppressed by `#[allow(dead_code)]` | **LOW** | DeadCode | 1.1, 1.2 | `src/config.rs:58`, `src/history.rs:39`, `src/audio/capability.rs:87`, `src/audio/volume.rs:77,152`, `src/audio/output_manager.rs:128,174,180`, `src/audio/supervisor.rs:106`, `src/app/ui_state.rs:37` | Test-only functions not gated with `#[cfg(test)]`; should be moved or removed |
| F-29 | Dead code: 6 enum variants/structs never constructed in production | **LOW** | DeadCode | 1.1 | `src/app/types.rs:68`, `src/audio/types.rs:16,59,117,163,231` | `DecoderState::Probing/Ended`, `EngineState::Recovering`, `EngineError::Abandoned`, `EndReason::Network/Decode`, `StreamFormat` — premature abstraction or incomplete features |
| F-30 | Clippy: `never_loop` in decode.rs prebuffer logic | **LOW** | DeadCode | 1.1 | `src/audio/decode.rs:422-448` | Loop body unconditionally breaks; dead control flow masquerading as a loop |
| F-31 | Unused dev-dependency: `tiny_http` | **LOW** | UnusedDependency | 1.3 | `Cargo.toml:47` | Zero references in codebase; adds unnecessary compile time to test builds |
| F-32 | KISS: Forwarding wrappers (3 instances) | **LOW** | KissViolation | 3.4 | `src/app/settings.rs:152`, `src/ui/settings.rs:191`, `src/favorites.rs:517` | Pass-through functions adding no logic; could be inlined at call sites |
| F-33 | KISS: `step_choice<T>` generic with single instantiation | **LOW** | KissViolation | 3.3 | `src/app/settings.rs:186` | Generic over `T: Copy + PartialEq` but only used with `ThemeName`; monomorphize |
| F-34 | Large type: `Action` enum (46 variants) | **LOW** | LargeType | 7.2 | `src/action.rs` | Largest enum in codebase; not on critical path but increases cognitive load for action dispatch |
| F-35 | Large type: `UiModel` (22 fields) | **LOW** | LargeType | 7.2 | `src/ui/model.rs` | Large but architecturally justified as read-only snapshot; not a functional concern |
| F-36 | Boundary: audio→app test-only coupling | **LOW** | BoundaryViolation | 6.4, 6.7 | `src/audio/types.rs:293,445` | Test modules import `classify_playback_error` from `crate::app`; no production coupling |
| F-37 | App infra fields at top level (`metadata_refresh_*`, `persist`) | **LOW** | SrpViolation | 4.1 | `src/app.rs` | Infrastructure coordination fields mixed with domain state at `App` top level; cosmetic grouping improvement |
| F-38 | DRY: Bitrate/health timestamp/empty-string helpers duplicated 2× each | **LOW** | DryViolation | 2.1 | `src/ui/stations.rs`, `src/ui/station_details.rs` | Minor 2-occurrence duplicates: `bitrate_chip`/`bitrate_label`, `station_health_failure_is_current`/`failure_is_after_success`, `empty_fallback`/`fallback` |
| F-39 | KISS: 15 functions at nesting depth 4 | **LOW** | KissViolation | 3.2 | Various (see 5.1 findings) | Multiple functions with moderate nesting; individually low priority but collectively increase cognitive load |
| F-40 | SRP: `PlaybackView.buffer_percent/buffer_seconds` are audio metrics in app-state struct | **LOW** | SrpViolation | 4.1 | `src/app/playback.rs:4` | Buffer fields duplicated between `PlaybackView` and `PlaybackDiagnostics`; minor boundary blur |

---

## Deduplication Notes

The following overlapping findings from multiple passes were merged:

| Merged Into | Originating Findings | Rationale |
|-------------|---------------------|-----------|
| F-12 + F-23 | 4.2 Pattern 1 (genre filtering DRY) + 7.1 Finding 2 (UI domain logic) | Same code duplication flagged as both DRY violation and boundary leak; root cause is identical |
| F-04 | 6.2 OCP visualizer + 6.2 numeric dispatch flags | Both describe the same underlying issue (raw usize visualizer mode) |
| F-14 + F-26 | 4.3 DRY-PB-01 (buffer reset) + 4.3 DRY-PB-06 (reconnect disarm) | Often co-occur but tracked separately as their extractions compose |
| F-07 | 7.1 Finding 1 (UiModel interior-mutable handle) + 9.2 architecture note | Same `Arc<Mutex>` concern from boundary and concurrency perspectives |
| F-24 | 3.2 observation (pub(crate) audio modules) + 6.3 encapsulation risk | Same pub(crate) visibility concern flagged by two passes |

---

## Priority Distribution

| Priority | Count | Percentage |
|----------|-------|------------|
| HIGH | 7 | 17.5% |
| MEDIUM | 19 | 47.5% |
| LOW | 14 | 35.0% |
| **Total** | **40** | 100% |

---

## Category Distribution

| Category | Count |
|----------|-------|
| DryViolation | 9 |
| KissViolation | 7 |
| BoundaryViolation | 4 |
| SrpViolation | 5 |
| OcpViolation | 2 |
| ErrorHandlingRisk | 3 |
| TestGap | 1 |
| EncapsulationRisk | 3 |
| CouplingHotspot | 1 |
| DeadCode | 3 |
| UnusedDependency | 1 |
| LargeType | 2 |

---

## Verification

- ✅ Minimum 10 distinct findings: **40 findings** with specific file paths, type names, and function names
- ✅ Test gaps and concurrency risks on critical paths classified as HIGH (F-03, audio thread `expect()` in F-01/F-02)
- ✅ Coupling hotspots (F-08) and encapsulation risks (F-22, F-24) classified as MEDIUM or higher
- ✅ All findings traceable to originating requirements (1–7)
- ✅ Findings deduplicated: overlapping items from multiple passes merged with rationale documented
