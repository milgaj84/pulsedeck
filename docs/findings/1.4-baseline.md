# Task 1.4 — Baseline Verification

**Timestamp:** 2025-07-14

## cargo build --release

| Metric | Result |
|--------|--------|
| Exit code | 0 |
| Errors | 0 |
| Warnings | 0 |

The codebase compiles cleanly under the release profile with zero errors and zero warnings.

## cargo test

| Metric | Result |
|--------|--------|
| Exit code | 0 |
| Tests run | 447 |
| Tests passed | 445 |
| Tests failed | 0 |
| Tests ignored | 2 |

All non-ignored tests pass. The 2 ignored tests are hardware-dependent audio output manager tests (`attach_creates_sink_and_is_not_drained_immediately`, `ensure_open_opens_device_on_first_call`) which require a physical audio device and are expected to be skipped in CI/headless environments.

### Compilation warnings during test build

One warning was emitted during the test profile compilation (not present in release):

```
warning: unused doc comment
   --> src/audio/volume.rs:305:5
    |
305 | /     /// **Property 8: Volume clamp**
306 | |     ///
307 | |     /// For any `f32` value `v` (including N...
308 | |     /// `clamp_volume(v)` always produces a ...
309 | |     ///
310 | |     /// **Validates: Requirements 10.6**
    | |_____-----------------------------------^
    |       |
    |       rustdoc does not generate documentation for macro invocations
    |
    = note: `#[warn(unused_doc_comments)]` on by default
```

This is a pre-existing doc-comment placement issue on a `proptest!` macro invocation and does not affect the release build or test correctness.

## Property 5 (Non-Destructiveness) — Baseline Evidence

The codebase is confirmed to be in a clean, compilable, and test-passing state prior to any audit analysis:

- **Release build:** PASS (zero errors, zero warnings)
- **Test suite:** PASS (445/445 non-ignored tests passing)
- **No source modifications:** This is the pre-audit baseline state

This baseline will be compared against the post-audit state (Task 11) to verify that the audit process made no code modifications and the codebase remains in an identical compilable/test-passing state.

---

**Requirements satisfied:**
- Req 1.5: Codebase compiles with zero errors and zero warnings under `cargo build --release` and passes all existing tests
- Req 8.6: Action plan does not modify codebase — baseline established for before/after comparison
