# Task 1.3 — Unused Dependencies and Feature Flags Audit

**Date:** Audit performed as part of Code Quality v0.5.1  
**Requirements:** 1.3, 1.7  
**Scope:** `Cargo.toml` `[dependencies]` and `[dev-dependencies]` cross-referenced against production source; `[features]` section cross-referenced against `cfg(feature = "...")` usage.

---

## Summary

| Category | Count |
|----------|-------|
| Production dependencies audited | 12 |
| Production dependencies confirmed used | 12 |
| Dependencies to demote/remove | 0 |
| Dev-dependencies audited | 2 |
| Dev-dependencies unused | 1 |
| Feature flags declared | 0 |
| Feature flags unused | 0 |

---

## Production Dependencies — All Confirmed Used

| Crate | Version | Usage Evidence |
|-------|---------|---------------|
| `ratatui` | 0.30 | Used extensively in `src/ui/` (18+ files), `src/main.rs` (`ratatui::init()`, `ratatui::restore()`) |
| `crossterm` | 0.29 | Used in `src/event.rs` (`crossterm::event::{Event, KeyCode, KeyEvent, KeyModifiers}`) |
| `tokio` | 1 (features: full) | `#[tokio::main]` in `src/main.rs`, `tokio::spawn` and `tokio::sync::mpsc` in `src/runtime.rs` |
| `rodio` | 0.20 (features: symphonia-all) | Used in `src/audio/decode.rs`, `src/audio/output.rs`, `src/audio/output_manager.rs`, `src/audio/volume.rs`, `src/audio/visualizer.rs`, `src/audio/types.rs` |
| `cpal` | 0.15 | Used in `src/audio/output.rs` (`cpal::default_host()`, `cpal::Device`, `cpal::traits::*`) |
| `libc` | 0.2 | Used in `src/audio/output.rs` under `#[cfg(unix)]` for stderr silencing (`libc::dup`, `libc::dup2`, `libc::close`, `libc::open`) |
| `reqwest` | 0.12 (features: json, blocking) | Used in `src/radio/client.rs` (async client) and `src/audio/decode.rs` (blocking client for stream fetch) |
| `serde` | 1 (features: derive) | `#[derive(Serialize, Deserialize)]` across `src/favorites.rs`, `src/history.rs`, `src/radio/station.rs`, `src/radio/map.rs`, `src/config.rs`, `src/app/types.rs`, `src/app/ui_state.rs` |
| `serde_json` | 1 | Used in `src/config.rs`, `src/favorites.rs`, `src/playlist.rs` for JSON serialization/deserialization |
| `anyhow` | 1 | Used in `src/main.rs`, `src/cli.rs`, `src/config.rs`, `src/favorites.rs`, `src/history.rs`, `src/radio/client.rs`, `src/radio.rs`, `src/playlist_export.rs`, `src/app/ui_state.rs` |
| `dirs` | 6 | Used in `src/config.rs` (`dirs::config_dir()`) and `src/app/playback.rs` |
| `notify-rust` | 4 | Used in `src/app/notifier.rs` (`notify_rust::Notification::new()`) |

**Verdict:** No production dependency should be demoted or removed. All are actively used in production code paths.

---

## Dev-Dependencies

| Crate | Version | Status | Evidence |
|-------|---------|--------|----------|
| `proptest` | 1 | ✅ **Used** | Used in `#[cfg(test)]` modules across `src/audio/volume.rs`, `src/audio/decode.rs`, `src/audio/types.rs`, `src/audio/supervisor.rs`, `src/audio/stream_source.rs` |
| `tiny_http` | 0.12 | ⚠️ **Unused** | No `use tiny_http`, `extern crate tiny_http`, or any type reference found anywhere in the codebase (production or test code) |

### Finding: DEP-001 — Unused dev-dependency `tiny_http`

- **Category:** `unused_dep`
- **Priority:** Low
- **Crate:** `tiny_http` v0.12
- **Location:** `Cargo.toml`, line 47 (`[dev-dependencies]` section)
- **Evidence:** Searched entire `src/` tree for `tiny_http`, `TinyHttp`, `tiny_http::Server`, and any type or function imported from this crate. Zero matches found.
- **Recommendation:** Remove `tiny_http = "0.12"` from `[dev-dependencies]`. It adds unused compile-time cost to `cargo test` builds.
- **Requirement:** 1.3 (AC 3: "flag the dependency for removal or demotion to `[dev-dependencies]`")
- **Verification:** Run `cargo test` after removal to confirm no test depends on it.

---

## Feature Flags

**No `[features]` section exists in `Cargo.toml`.** There are zero custom feature flags declared for this crate.

A search for `cfg(feature = "...")` across the entire `src/` tree returned zero matches.

**Verdict:** Requirement 1.7 is satisfied — there are no declared-but-unused feature flags to report.

---

## Dependency Feature Activations (Informational)

The following dependency feature flags are activated and confirmed used:

| Dependency | Feature | Usage Justification |
|------------|---------|---------------------|
| `tokio` | `full` | async runtime, `mpsc` channels, `spawn` — all actively used |
| `rodio` | `symphonia-all` | Symphonia-based audio decoding for MP3, AAC, OGG, Opus, FLAC, WAV |
| `reqwest` | `json` | `.json()` response deserialization in `src/radio/client.rs` |
| `reqwest` | `blocking` | Blocking HTTP client for audio stream fetch in `src/audio/decode.rs` |
| `serde` | `derive` | `#[derive(Serialize, Deserialize)]` used across 7+ modules |

**Note:** `tokio` with `features = ["full"]` is broader than strictly necessary (the project uses `rt-multi-thread`, `macros`, and `sync`). This is a minor optimization opportunity but not a correctness issue and is common practice for application crates.

---

## Conclusion

The project's dependency hygiene is excellent. Only one finding warrants action:

1. **DEP-001:** Remove unused dev-dependency `tiny_http` (Low priority)

All production dependencies are actively used and correctly categorized. No feature flag issues exist.
